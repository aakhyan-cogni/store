# store

## Agent skills

### Issue tracker

Local markdown under `.scratch/` — the repo is WIP and does not use GitHub Issues yet; the `gh` CLI is unavailable, so never call it. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary, label strings unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
